// a class to print user selected emoji and time
class EmotionEvent {
  final int? id;
  final String emoji;
  final DateTime date;
  final int point;

  // ctor
  EmotionEvent(this.id, this.emoji, this.date, this.point);
}