class WorkoutEvent {
  final int? id;
  final String workout;
  final int unit;
  final DateTime date;
  final int point;

  // ctor
  WorkoutEvent(this.id, this.workout, this.unit, this.date, this.point);
}